// Pattern: TodoItemDetailPage code-behind — minimal.
// Entity data and actions are managed by TodoItemDetailModel (MVUX).

namespace TaskFlow.UI.Views;

public sealed partial class TodoItemDetailPage : Page
{
    public TodoItemDetailPage() => InitializeComponent();
}
