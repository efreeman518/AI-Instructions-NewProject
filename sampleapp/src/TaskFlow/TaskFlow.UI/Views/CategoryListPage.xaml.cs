// Pattern: CategoryListPage code-behind — minimal.
// List data and observable refresh are managed by CategoryListModel (MVUX).

namespace TaskFlow.UI.Views;

public sealed partial class CategoryListPage : Page
{
    public CategoryListPage() => InitializeComponent();
}
